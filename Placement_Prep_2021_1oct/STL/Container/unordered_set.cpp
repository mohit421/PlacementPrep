//                              8 Feb,2022

/*

- it can store element in any order 
- Order doesn't matter

// declaration:-

unordered_set<int> st;
st.insert(2);
st.insert(3);
st.insert(1);


//  Average time complexity is O(1).
//  but the worst case is linear in nature :- O(set_size)
// If TLE then switch to --> set

// Implementation is same as set

